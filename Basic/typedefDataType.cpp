#include<iostream>
using namespace std;
/*
used to define our own datatype or giving alias to some datatype
used to make lengthy programs more readable
define above main function
*/

typedef int marks;

int main(){
    marks m1, m2;
    // m1 and m2 are marks type and marks is integer type
    // used to make program more readable
    // need to use comment for letting know the users that m1 and m2 are marks when define wiyh int directly
    m1=50;
    m2=90;

    return 0;
}